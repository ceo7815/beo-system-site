# Google Analytics 4. Also set this in Cloudflare Pages → Environment variables
# so the static export inlines it at build time. Loaded only after analytics consent.
NEXT_PUBLIC_GA_MEASUREMENT_ID=G-FNQR0XYR54
